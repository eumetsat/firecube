[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "direct-zarr-capable-test-plugin"
version = "0.1.0"
dependencies = ["numpy"]

[project.entry-points."firecube.plugins"]
direct_zarr_capable_test_plugin = "direct_zarr_capable_test_plugin"
